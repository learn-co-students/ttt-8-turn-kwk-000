#!/usr/bin/env ruby
require_relative '../lib/turn.rb'

board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]
input = gets.strip
index = input_to_index(input)

puts "Welcome to Tic Tac Toe!"
display_board(board)

turn(board)


move(board, index, "X")
display_board(board)
